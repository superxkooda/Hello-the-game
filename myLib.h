#ifndef myLib_h
#define myLib_h

#include "common.h"

#ifndef _WIN32
struct COORD
{
    int X;
    int Y;
};
#endif

//int animate(COORD start);


//void animate(COORD &start, COORD &finish, char obj[]);// WINDOW win);

#endif
